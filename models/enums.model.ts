/* eslint-disable no-unused-vars */

export enum UserRoles {
  Admin = "admin",
  User = "user",
}

export enum UserTypes {
  Admin = "admin",
  User = "user",
  Employee = "employee",
}

export enum EmployeeRoles {
  Worker = "worker",
  Manager = "manager",
  Director = "director",
}

export enum Lang {
  en = "en",
  ua = "ua",
}
